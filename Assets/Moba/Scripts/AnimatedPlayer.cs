using System.Net.Security;
using UnityEngine;

namespace Moba
{
    public class AnimatedPlayer : BasePlayer
    {
        private Animator _animator;

        protected override void Awake()
        {
            base.Awake();
            _animator = GetComponentInChildren<Animator>();

        protected override BeActive()
    }
}


}